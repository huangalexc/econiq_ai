# infrastructure

Terraform for the AWS deployment described in tech rec §29–§30 (CloudFront/ALB,
ECS Fargate, RDS, ElastiCache, SQS/SNS).

Empty until issue #70, which the audit scheduled for the start of Phase 1 —
Phase 0 runs entirely on the local Docker Compose stack.
