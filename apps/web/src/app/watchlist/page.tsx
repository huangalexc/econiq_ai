import { Placeholder } from "@/components/placeholder";

export default function WatchlistPage() {
  return (
    <Placeholder title="Watchlist" issue="#32">
      <p>Saved Processes and Assets with semantic alerts, driven by thesis-invalidation signals rather than price moves.</p>
    </Placeholder>
  );
}
