import { Placeholder } from "@/components/placeholder";

export default function DependencyPage() {
  return (
    <Placeholder title="Dependency graph" issue="#26">
      <p>The React Flow explorer over Process, Bottleneck, Capability and Asset, walking the same typed edges the traversal layer enforces.</p>
    </Placeholder>
  );
}
