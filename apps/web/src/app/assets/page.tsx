import { Placeholder } from "@/components/placeholder";

export default function AssetsPage() {
  return (
    <Placeholder title="Assets" issue="#28, #29, #30">
      <p>The Asset page with its discovery chain, the comparison matrix, and the underwriting screen — why this, why now, why not.</p>
    </Placeholder>
  );
}
